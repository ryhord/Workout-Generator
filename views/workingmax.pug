extends layout

block content
  div( class="main-container" )
    div( class="content-container" )
      h1 Your Working Max
      h2= workingmax
      br
      br
      form(method="GET" action="/workingmax/results")
        button(type="submit" class="btn btn-outline-light") Get Results